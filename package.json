{
    "name": "wordparrot-spawn-instance",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "start": "",
        "update": "",
        "build": "tsc --outDir dist/ src/*",
        "prune-static-volume": "",
        "generate-env": "node ./dist/generateEnv.js",
        "bss": "docker-compose build --no-cache sites_server",
        "bn": "docker-compose build --no-cache nginx"
    },
    "devDependencies": {
        "@types/node": "^18.11.9",
        "typescript": "^4.2.0"
    }
}
